1. whatever we write in jsx are react elements not html elements or tags.
2. JSX is javascript extension. It is not necessary to use JSX in React development, but it is recomended.
3. undefined , null , true , false are ignored by JSX.
4. JSX does not have built in data binding.We don't render a single thing to the screen until we call
   reactDOM.render
5. we can render JSX in JSX.
6. We can render array of JSX in JSX.